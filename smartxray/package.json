{
  "name": "smartxray",
  "version": "2.1.0",
  "description": "Xray server manager with SQLite, dynamic config, firewall & Web UI",
  "main": "src/xray-ctl",
  "scripts": {
    "start":   "node src/xray-ctl start",
    "pack":    "node scripts/release.js",
    "release": "node scripts/release.js --publish"
  },
  "dependencies": {
    "better-sqlite3": "^9.4.3",
    "nodemailer": "^6.9.9"
  },
  "engines": {
    "node": ">=16"
  }
}
